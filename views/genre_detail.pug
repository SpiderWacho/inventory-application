extends layout

block content
    h1 Genre: #{genre.name}
    h2 #[strong Games in this genre:] 
    if (gamesByGenre.length) 
        each game in gamesByGenre 
            a(href=game.url) #{game.title}
    else 
        p There is no games in this genre.
    