extends layout

block content
  div.container
    div.list-content
      h1.title= title

      div.list-container 
        ul
          each genre in genre_list
            li
              a(href=genre.url) #{genre.name}

          else
            li There are no genres created.


      button.btn-primary
        a(href='/catalog/genre/create') Add a genre